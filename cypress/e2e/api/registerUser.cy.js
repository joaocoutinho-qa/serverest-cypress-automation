describe('template spec', () => {
  it('Register administrator user with valid credentials', () => {
    
  })

  it('Try to register admin user with already registered email', () => {

  })

  it('Register administrator user without entering a name', () => {

  })
})
